# Sidecar (Node runtime)
PORT=4318
OLLAMA_BASE_URL=http://127.0.0.1:11434
OCW_DB_PATH=./ocw_assistant.db

# Google OAuth desktop app credentials (Google Cloud Console)
# Required for Google Calendar + Gmail features.
# Create an OAuth Client ID and enable:
# - Google Calendar API
# - Gmail API
# Add this redirect URI to the OAuth client:
#   http://127.0.0.1:8765/oauth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://127.0.0.1:8765/oauth/google/callback

# Optional keytar service name override (OS keychain/keytar namespace)
# OCW_TOKEN_SERVICE=ocw-assistant

# UI (Vite / React)
VITE_SIDECAR_URL=http://127.0.0.1:4318
